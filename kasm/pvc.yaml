# Kasm stores Docker layers + install data in /opt. 30Gi as requested.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kasm-opt
  namespace: kasm
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 30Gi
